-- Vocabulary is intentionally empty until a versioned, approved taxonomy is supplied.
select cast(null as varchar) as allergen_key, cast(null as varchar) as name, cast(null as varchar) as taxonomy_version
where false
