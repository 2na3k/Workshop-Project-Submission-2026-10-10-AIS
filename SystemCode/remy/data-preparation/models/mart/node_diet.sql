-- Vocabulary is intentionally empty until a versioned, approved policy is supplied.
select cast(null as varchar) as diet_key, cast(null as varchar) as name, cast(null as varchar) as policy_version
where false
